A small, accessible, responsive Profile Card component/page using semantic HTML and modern frontend patterns (HTML/CSS/vanilla JS only). 
Every visible element includes a data-testid attribute so automated tests can target it.
